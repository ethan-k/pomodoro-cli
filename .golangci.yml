run:
  timeout: 3m
  issues-exit-code: 1
  tests: true
  skip-dirs:
    - vendor

output:
  format: colored-line-number
  print-issued-lines: true
  print-linter-name: true

linters-settings:
  goimports:
    local-prefixes: github.com/ethan-k/pomodoro-cli
  
  gocritic:
    enabled-checks:
      - nestingReduce
      - unnamedResult
      - ruleguard
      - truncateCmp
  
  govet:
    check-shadowing: true
  
  misspell:
    locale: US
  
  gocyclo:
    min-complexity: 15

linters:
  disable-all: true
  enable:
    - goimports
    - golint
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - structcheck
    - varcheck
    - ineffassign
    - deadcode
    - typecheck
    - gosec
    - gocyclo
    - gofmt
    - misspell
    - gocritic

issues:
  exclude-rules:
    # Exclude some linters from running on tests files.
    - path: _test\.go
      linters:
        - gocyclo
        - errcheck
        - gosec
    
    # Ignore long lines in generated files
    - path: cmd/root.go
      linters:
        - lll
    
    # Ignore certain golint issues
    - linters:
        - golint
      text: "should have comment"
  
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0